App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =ClearCollect(
            ListaInventarioLocal,
            'Lista Inventarios'
        );
        ClearCollect(
            ListaGestionLocal,
            'Lista Gestión Inventarios'
        )
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

